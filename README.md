# roboshop-ci-cd-v1

1. each pipeline has workflows
2. each workflows has a jobs
3. each job has one runner[build is one job, test is another job,....]
4. each job has steps

ci runner is a github action runner 
* first run manually 
* make steps and keep these github action runner inside a container
* 
